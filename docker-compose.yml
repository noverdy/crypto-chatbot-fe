version: "3"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    user: "node"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
    ports:
      - "${PORT}:5173"